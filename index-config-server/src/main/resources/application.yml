spring:
  application:
    name: index-config-server
  cloud:
    config:
      label: main
      server:
        git:
          uri: https://github.com/FangYuanLL/trendConfigVersion/
          searchPaths: config
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/